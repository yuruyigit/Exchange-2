@import '~assets/stylus/variable';
.hot {
    &_list {
        padding: 14px 21px;
        &_single {
            display: block;
            width: 100%;
            height: 106px;
            border-radius: 4px;
            overflow: hidden;
            margin-bottom: 14px;
            img {
                width: 100%;
                height: 100%;
            }
        }
    }
}